---
description: Analyze requirements, create/maintain FSD and use cases
---
# /specs - Business Analysis

$ARGUMENTS

---

## Task

Load and follow the skill definition at `.agent/skills/specs/SKILL.md`.

Pass `$ARGUMENTS` to the skill as the subcommand and arguments.

---

## Flags

| Flag | Description |
|------|-------------|
| `init` | Analyze codebase & create initial FSD + use cases |
| `analyze "description"` | Analyze new feature requirements |
| `update` | Sync FSD & use cases with codebase |
